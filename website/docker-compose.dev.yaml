services:
  website:
    container_name: web-dev
    build:
      context: .
      dockerfile: ./docker/dev/Dockerfile
    env_file:
      - ./docker/dev/.env
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./app:/app
    restart: always
    networks:
      - backend-network

networks:
  backend-network:
    external: true